# 護照辨識系統 - 元件圖

本文件展示護照辨識系統的元件結構與依賴關係。

## 系統元件圖

```mermaid
graph TB
    subgraph "應用層 (Application Layer)"
        API[Flask API<br/>app.py]
        BATCH[批次處理器<br/>main.py]
        EXAMPLE[使用範例<br/>example.py]
    end
    
    subgraph "護照辨識系統"
        subgraph "控制層 (Controller Layer)"
            PC[PassportController<br/>主控制器]
            PS[PassportService<br/>API 服務]
            BP[PassportBatchProcessor<br/>批次處理器]
        end
        
        subgraph "業務邏輯層 (Business Logic Layer)"
            IE[ImageEncoder<br/>圖片編碼器]
            VA[VisionAnalyzer<br/>圖像分析器]
            RP[ResultParser<br/>結果解析器]
        end
        
        subgraph "資料層 (Data Layer)"
            PT[PromptTemplates<br/>提示詞模板]
            PF[PassportField<br/>欄位枚舉]
        end
        
        subgraph "外部服務 (External Services)"
            GAPI[Google Gemini API<br/>圖像理解服務]
        end
    end
    
    API --> PS
    BATCH --> BP
    EXAMPLE --> PC
    
    PS --> VA
    PS --> RP
    BP --> PC
    PC --> IE
    PC --> VA
    PC --> RP
    VA --> PT
    VA --> GAPI
    VA --> PF
    RP --> PF
    PT --> PF
```

## 依賴關係圖

```mermaid
graph LR
    subgraph "應用程式"
        APP[app.py]
        MAIN[main.py]
        EX[example.py]
    end
    
    subgraph "核心套件 src/"
        PC[passport_controller.py]
        PS[passport_service.py]
        IE[image_encoder.py]
        VA[vision_analyzer.py]
        RP[result_parser.py]
        PT[prompt_templates.py]
    end
    
    subgraph "外部依賴"
        FLASK[Flask<br/>Web 框架]
        PIL[Pillow<br/>圖片處理]
        GENAI[google-genai<br/>Gemini SDK]
        STD[Python 標準庫<br/>base64, json, pathlib, csv]
    end
    
    APP -.依賴.-> PS
    APP -.依賴.-> FLASK
    MAIN -.依賴.-> PC
    MAIN -.依賴.-> STD
    EX -.依賴.-> PC
    
    PC -.依賴.-> IE
    PC -.依賴.-> VA
    PC -.依賴.-> RP
    PS -.依賴.-> VA
    PS -.依賴.-> RP
    VA -.依賴.-> PT
    RP -.依賴.-> PT
    
    IE -.使用.-> STD
    VA -.使用.-> GENAI
    VA -.使用.-> PIL
    VA -.使用.-> PT
    RP -.使用.-> STD
```

## 資料流向圖

### 從檔案路徑辨識（PassportController）

```mermaid
graph TD
    A[護照圖片檔案] --> B[ImageEncoder]
    A --> C[VisionAnalyzer]
    
    B --> D[Base64 編碼<br/>MIME 類型]
    
    E[PromptTemplates] --> C
    C --> F[Gemini API]
    
    F --> G[LLM 回應<br/>JSON 字串]
    
    G --> H[ResultParser]
    
    H --> I[結構化護照資料<br/>Python Dict]
    
    I --> J[使用者程式]
    
    style A fill:#e1f5ff
    style I fill:#c8e6c9
    style F fill:#fff3e0
```

### 從 BASE64 辨識（PassportService）

```mermaid
graph TD
    A[BASE64 編碼圖片] --> B[PassportService]
    
    B --> C[解碼為位元組資料]
    
    C --> D[VisionAnalyzer]
    
    E[PromptTemplates] --> D
    D --> F[Gemini API]
    
    F --> G[LLM 回應<br/>JSON 字串]
    
    G --> H[ResultParser]
    
    H --> I[結構化護照資料<br/>Python Dict]
    
    I --> J[Flask API 回應]
    
    style A fill:#e1f5ff
    style I fill:#c8e6c9
    style F fill:#fff3e0
```

## 元件說明

### 應用層
- **app.py**: Flask API 應用程式，提供 HTTP 介面供外部系統呼叫護照辨識服務
- **main.py**: 批次處理應用程式，可批次處理目錄下的所有護照圖片並輸出 CSV
- **example.py**: 使用範例程式，展示如何使用護照辨識系統

### 控制層
- **PassportController**: 主控制器，協調所有模組的交互作用，支援從本地檔案進行辨識
- **PassportService**: API 服務層，專門處理 BASE64 編碼圖片的辨識，適用於 Web API
- **PassportBatchProcessor**: 批次處理器，負責掃描目錄、批次辨識和 CSV 輸出

### 業務邏輯層
- **ImageEncoder**: 負責圖片編碼和格式驗證
- **VisionAnalyzer**: 負責呼叫 Gemini API 進行圖像理解，支援檔案和位元組資料
- **ResultParser**: 負責解析和結構化 LLM 的回應

### 資料層
- **PromptTemplates**: 管理所有提示詞模板
- **PassportField**: 定義護照欄位的枚舉類型

### 外部服務
- **Google Gemini API**: 提供圖像理解和文字辨識功能

## 部署架構

```mermaid
graph TB
    subgraph "客戶端"
        WEB[Web 應用程式<br/>或其他系統]
        CLI[命令列工具<br/>批次處理]
    end
    
    subgraph "本地或伺服器環境"
        API[Flask API<br/>app.py:5000]
        BATCH[批次處理器<br/>main.py]
        CORE[護照辨識核心<br/>src/]
    end
    
    subgraph "Google Cloud"
        GEMINI[Gemini API Service<br/>圖像理解]
    end
    
    WEB -->|HTTP POST<br/>BASE64 Image| API
    CLI -->|執行命令| BATCH
    
    API --> CORE
    BATCH --> CORE
    CORE -->|HTTPS API Call| GEMINI
    GEMINI -->|JSON Response| CORE
    
    style API fill:#e3f2fd
    style BATCH fill:#e3f2fd
    style CORE fill:#c8e6c9
    style GEMINI fill:#fff9c4
```
