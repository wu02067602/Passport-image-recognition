# 護照辨識系統 - 元件圖

本文件展示護照辨識系統的元件結構與依賴關係。

## 系統元件圖

```mermaid
graph TB
    subgraph "應用層 (Application Layer)"
        API[Flask API<br/>app.py<br/>async 非同步]
        SINGLE[單張辨識端點<br/>/api/passport/recognize]
        BATCH[批次辨識端點<br/>/api/passport/recognize/batch]
    end
    
    subgraph "護照辨識系統"
        subgraph "服務層 (Service Layer)"
            PS[PassportService<br/>非同步 API 服務]
        end
        
        subgraph "業務邏輯層 (Business Logic Layer)"
            VA[VisionAnalyzer<br/>圖像分析器<br/>並發執行]
            RP[ResultParser<br/>結果解析器]
        end
        
        subgraph "資料層 (Data Layer)"
            PT[PromptTemplates<br/>提示詞模板]
            PF[PassportField<br/>欄位枚舉]
        end
        
        subgraph "外部服務 (External Services)"
            GAPI[Google Gemini API<br/>圖像理解服務]
        end
    end
    
    API --> SINGLE
    API --> BATCH
    SINGLE --> PS
    BATCH --> PS
    PS --> VA
    PS --> RP
    VA --> PT
    VA --> PF
    VA --> GAPI
    RP --> PF
    PT --> PF
```

## 依賴關係圖

```mermaid
graph LR
    subgraph "應用程式"
        APP[app.py<br/>async Flask API]
    end
    
    subgraph "核心套件 src/"
        PSVC[passport_service.py<br/>async 服務]
        VA[vision_analyzer.py<br/>async 並發]
        RP[result_parser.py]
        PT[prompt_templates.py]
    end
    
    subgraph "外部依賴"
        FLASK[Flask<br/>Web 框架]
        PIL[Pillow<br/>圖片處理]
        GENAI[google-genai<br/>Gemini SDK]
        STD[Python 標準庫<br/>base64, json, asyncio]
    end
    
    APP -.依賴.-> PSVC
    APP -.依賴.-> FLASK
    
    PSVC -.依賴.-> VA
    PSVC -.依賴.-> RP
    VA -.依賴.-> PT
    RP -.依賴.-> PT
    
    VA -.使用.-> GENAI
    VA -.使用.-> PIL
    VA -.使用.-> PT
    RP -.使用.-> STD
    PSVC -.使用.-> STD
```

## 資料流向圖

### 單張辨識流程（/api/passport/recognize）

```mermaid
graph TD
    A[BASE64 編碼圖片] --> B[PassportService]
    
    B --> C[解碼為位元組資料]
    
    C --> D[VisionAnalyzer]
    
    E[PromptTemplates] --> D
    D --> F[Gemini API]
    
    F --> G[LLM 回應<br/>JSON 字串]
    
    G --> H[ResultParser]
    
    H --> I[結構化護照資料<br/>Python Dict]
    
    I --> J[Flask API 回應]
    
    style A fill:#e1f5ff
    style I fill:#c8e6c9
    style F fill:#fff3e0
```

### 批次辨識流程（/api/passport/recognize/batch）

```mermaid
graph TD
    A[多張 BASE64 編碼圖片<br/>images 陣列] --> B[分批處理<br/>每批次 20 張]
    
    B --> C1[批次 1]
    B --> C2[批次 2]
    B --> C3[批次 N...]
    
    C1 --> D[asyncio.gather<br/>並發辨識]
    C2 --> D
    C3 --> D
    
    D --> E1[PassportService<br/>圖片 1]
    D --> E2[PassportService<br/>圖片 2]
    D --> E3[PassportService<br/>圖片 N...]
    
    E1 --> F[收集所有結果]
    E2 --> F
    E3 --> F
    
    F --> G[統計成功/失敗數量]
    
    G --> H[Flask API 回應<br/>results 陣列]
    
    style A fill:#e1f5ff
    style D fill:#fff3e0
    style H fill:#c8e6c9
```

## 元件說明

### 應用層
- **app.py**: Flask API 應用程式，提供非同步 HTTP 介面供外部系統呼叫護照辨識服務

### 服務層
- **PassportService**: 非同步 API 服務層，專門處理 BASE64 編碼圖片的辨識，使用 async/await 模式提升執行效率

### 業務邏輯層
- **VisionAnalyzer**: 負責呼叫 Gemini API 進行圖像理解，處理圖片位元組資料，使用 `asyncio.gather()` 並發執行所有欄位的分析
- **ResultParser**: 負責解析和結構化 LLM 的回應

### 資料層
- **PromptTemplates**: 管理所有提示詞模板
- **PassportField**: 定義護照欄位的枚舉類型

### 外部服務
- **Google Gemini API**: 提供圖像理解和文字辨識功能

## 部署架構

```mermaid
graph TB
    subgraph "客戶端"
        WEB[Web / API 客戶端]
    end
    
    subgraph "本地或伺服器環境"
        API[Flask API<br/>app.py<br/>async]
        CORE[PassportService<br/>src/<br/>async]
    end
    
    subgraph "Google Cloud"
        GEMINI[Gemini API Service<br/>圖像理解]
    end
    
    WEB -->|HTTP POST<br/>BASE64 Image| API
    
    API --> CORE
    CORE -->|HTTPS API Call<br/>並發請求| GEMINI
    GEMINI -->|JSON Response| CORE
    
    style API fill:#e3f2fd
    style CORE fill:#c8e6c9
    style GEMINI fill:#fff9c4
```
