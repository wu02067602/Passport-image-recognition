# 護照辨識系統 - 元件圖

本文件展示護照辨識系統的元件結構與依賴關係。

## 系統元件圖

```mermaid
graph TB
    subgraph "護照辨識系統"
        subgraph "控制層 (Controller Layer)"
            PC[PassportController<br/>主控制器]
        end
        
        subgraph "業務邏輯層 (Business Logic Layer)"
            IE[ImageEncoder<br/>圖片編碼器]
            VA[VisionAnalyzer<br/>圖像分析器]
            RP[ResultParser<br/>結果解析器]
        end
        
        subgraph "資料層 (Data Layer)"
            PT[PromptTemplates<br/>提示詞模板]
            PF[PassportField<br/>欄位枚舉]
        end
        
        subgraph "外部服務 (External Services)"
            GAPI[Google Gemini API<br/>圖像理解服務]
        end
    end
    
    subgraph "使用者介面 (User Interface)"
        USER[使用者程式<br/>example.py]
    end
    
    USER --> PC
    PC --> IE
    PC --> VA
    PC --> RP
    VA --> PT
    VA --> GAPI
    VA --> PF
    RP --> PF
    PT --> PF
```

## 依賴關係圖

```mermaid
graph LR
    subgraph "核心套件 src/"
        PC[passport_controller.py]
        IE[image_encoder.py]
        VA[vision_analyzer.py]
        RP[result_parser.py]
        PT[prompt_templates.py]
    end
    
    subgraph "外部依賴"
        PIL[Pillow<br/>圖片處理]
        GENAI[google-generativeai<br/>Gemini SDK]
        STD[Python 標準庫<br/>base64, json, pathlib]
    end
    
    PC -.依賴.-> IE
    PC -.依賴.-> VA
    PC -.依賴.-> RP
    VA -.依賴.-> PT
    RP -.依賴.-> PT
    
    IE -.使用.-> STD
    VA -.使用.-> GENAI
    VA -.使用.-> PIL
    VA -.使用.-> PT
    RP -.使用.-> STD
```

## 資料流向圖

```mermaid
graph TD
    A[護照圖片檔案] --> B[ImageEncoder]
    A --> C[VisionAnalyzer]
    
    B --> D[Base64 編碼<br/>MIME 類型]
    
    E[PromptTemplates] --> C
    C --> F[Gemini API]
    
    F --> G[LLM 回應<br/>JSON 字串]
    
    G --> H[ResultParser]
    
    H --> I[結構化護照資料<br/>Python Dict]
    
    I --> J[使用者程式]
    
    style A fill:#e1f5ff
    style I fill:#c8e6c9
    style F fill:#fff3e0
```

## 元件說明

### 控制層
- **PassportController**: 主控制器，協調所有模組的交互作用

### 業務邏輯層
- **ImageEncoder**: 負責圖片編碼和格式驗證
- **VisionAnalyzer**: 負責呼叫 Gemini API 進行圖像理解
- **ResultParser**: 負責解析和結構化 LLM 的回應

### 資料層
- **PromptTemplates**: 管理所有提示詞模板
- **PassportField**: 定義護照欄位的枚舉類型

### 外部服務
- **Google Gemini API**: 提供圖像理解和文字辨識功能

## 部署架構

```mermaid
graph TB
    subgraph "本地環境"
        APP[Python 應用程式<br/>護照辨識系統]
        IMG[圖片檔案<br/>JPG/PNG]
    end
    
    subgraph "Google Cloud"
        GEMINI[Gemini API Service<br/>圖像理解]
    end
    
    IMG --> APP
    APP -->|HTTPS API Call| GEMINI
    GEMINI -->|JSON Response| APP
    
    style APP fill:#e3f2fd
    style GEMINI fill:#fff9c4
```
