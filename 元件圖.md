# 護照辨識系統 - 元件圖

本文件展示護照辨識系統的元件結構與依賴關係。

## 系統元件圖

```mermaid
graph TB
    subgraph "應用層 (Application Layer)"
        API[Flask API<br/>app.py]
        BATCH[批次處理器<br/>main.py]
        EXAMPLE[使用範例<br/>example.py]
    end
    
    subgraph "護照辨識系統"
        subgraph "服務層 (Service Layer)"
            PS[PassportService<br/>Base64 API 服務]
            BP[PassportBatchProcessor<br/>批次協調器]
        end
        
        subgraph "業務邏輯層 (Business Logic Layer)"
            VA[VisionAnalyzer<br/>圖像分析器]
            RP[ResultParser<br/>結果解析器]
        end
        
        subgraph "資料層 (Data Layer)"
            PT[PromptTemplates<br/>提示詞模板]
            PF[PassportField<br/>欄位枚舉]
        end
        
        subgraph "外部服務 (External Services)"
            GAPI[Google Gemini API<br/>圖像理解服務]
        end
    end
    
    API --> PS
    BATCH --> BP
    EXAMPLE --> PS
    
    BP --> PS
    PS --> VA
    PS --> RP
    VA --> PT
    VA --> PF
    VA --> GAPI
    RP --> PF
    PT --> PF
```

## 依賴關係圖

```mermaid
graph LR
    subgraph "應用程式"
        APP[app.py]
        MAIN[main.py]
        EX[example.py]
    end
    
    subgraph "核心套件 src/"
        PSVC[passport_service.py]
        VA[vision_analyzer.py]
        RP[result_parser.py]
        PT[prompt_templates.py]
    end
    
    subgraph "外部依賴"
        FLASK[Flask<br/>Web 框架]
        PIL[Pillow<br/>圖片處理]
        GENAI[google-genai<br/>Gemini SDK]
        STD[Python 標準庫<br/>base64, json, pathlib, csv]
    end
    
    APP -.依賴.-> PSVC
    APP -.依賴.-> FLASK
    MAIN -.依賴.-> PSVC
    MAIN -.依賴.-> STD
    EX -.依賴.-> PSVC
    
    PSVC -.依賴.-> VA
    PSVC -.依賴.-> RP
    VA -.依賴.-> PT
    RP -.依賴.-> PT
    
    VA -.使用.-> GENAI
    VA -.使用.-> PIL
    VA -.使用.-> PT
    RP -.使用.-> STD
    PSVC -.使用.-> STD
```

## 資料流向圖

### API / BASE64 即時辨識（PassportService）

```mermaid
graph TD
    A[BASE64 編碼圖片] --> B[PassportService]
    
    B --> C[解碼為位元組資料]
    
    C --> D[VisionAnalyzer]
    
    E[PromptTemplates] --> D
    D --> F[Gemini API]
    
    F --> G[LLM 回應<br/>JSON 字串]
    
    G --> H[ResultParser]
    
    H --> I[結構化護照資料<br/>Python Dict]
    
    I --> J[Flask API 回應]
    
    style A fill:#e1f5ff
    style I fill:#c8e6c9
    style F fill:#fff3e0
```

### 批次處理（PassportBatchProcessor）

```mermaid
graph TD
    A[圖片檔案目錄] --> B[PassportBatchProcessor]
    B --> C[檔案掃描與副檔名檢查]
    C --> D[讀取檔案並轉成 BASE64]
    D --> E[PassportService]
    E --> F[base64.b64decode]
    F --> G[VisionAnalyzer]
    H[PromptTemplates] --> G
    G --> I[Gemini API]
    I --> J[LLM 回應 JSON]
    J --> K[ResultParser]
    K --> L[結構化護照資料]
    L --> M[CSV 輸出]
    
    style A fill:#e1f5ff
    style M fill:#c8e6c9
    style I fill:#fff3e0
```

## 元件說明

### 應用層
- **app.py**: Flask API 應用程式，提供 HTTP 介面供外部系統呼叫護照辨識服務
- **main.py**: 批次處理應用程式，將圖片轉為 BASE64 後呼叫服務並輸出 CSV
- **example.py**: 使用範例程式，展示如何將檔案轉為 BASE64 並呼叫服務

### 服務層
- **PassportService**: API 服務層，專門處理 BASE64 編碼圖片的辨識，適用於 Web API 與批次情境
- **PassportBatchProcessor**: 批次處理器，負責掃描目錄、轉換 BASE64、呼叫服務並輸出 CSV

### 業務邏輯層
- **VisionAnalyzer**: 負責呼叫 Gemini API 進行圖像理解，處理圖片位元組資料
- **ResultParser**: 負責解析和結構化 LLM 的回應

### 資料層
- **PromptTemplates**: 管理所有提示詞模板
- **PassportField**: 定義護照欄位的枚舉類型

### 外部服務
- **Google Gemini API**: 提供圖像理解和文字辨識功能

## 部署架構

```mermaid
graph TB
    subgraph "客戶端"
        WEB[Web / API 客戶端]
        CLI[命令列工具<br/>批次處理]
    end
    
    subgraph "本地或伺服器環境"
        API[Flask API<br/>app.py]
        BATCH[PassportBatchProcessor<br/>main.py]
        CORE[PassportService<br/>src/]
    end
    
    subgraph "Google Cloud"
        GEMINI[Gemini API Service<br/>圖像理解]
    end
    
    WEB -->|HTTP POST<br/>BASE64 Image| API
    CLI -->|讀取檔案→BASE64| BATCH
    
    API --> CORE
    BATCH --> CORE
    CORE -->|HTTPS API Call| GEMINI
    GEMINI -->|JSON Response| CORE
    
    style API fill:#e3f2fd
    style BATCH fill:#e3f2fd
    style CORE fill:#c8e6c9
    style GEMINI fill:#fff9c4
```
