# 護照辨識系統 - 元件圖

本文件展示護照辨識系統的元件架構與依賴關係。

## 系統元件架構

```mermaid
graph TB
    subgraph 使用者層
        A[使用者應用程式]
        B[example.py]
    end
    
    subgraph 控制層
        C[PassportRecognitionController]
    end
    
    subgraph 服務層
        D[GeminiImageRecognizer]
        E[ResponseParser]
    end
    
    subgraph 資料層
        F[PassportRecognitionResult]
    end
    
    subgraph 外部服務
        G[Google Gemini API]
    end
    
    subgraph 檔案系統
        H[圖片檔案]
    end
    
    A --> C
    B --> C
    C --> D
    C --> E
    D --> G
    D --> H
    E --> F
    C --> F
```

## 元件說明

### 使用者層
- **使用者應用程式**: 呼叫護照辨識系統的外部應用程式
- **example.py**: 系統使用範例程式

### 控制層
- **PassportRecognitionController**: 協調辨識流程的控制器，負責整合各個服務元件

### 服務層
- **GeminiImageRecognizer**: 影像辨識服務，負責與 Gemini API 互動
- **ResponseParser**: 回應解析服務，負責解析 LLM 回傳的結果

### 資料層
- **PassportRecognitionResult**: 封裝辨識結果的資料物件

### 外部服務
- **Google Gemini API**: 提供影像辨識能力的外部 API 服務

### 檔案系統
- **圖片檔案**: 需要辨識的護照圖片檔案

## 資料流向

```mermaid
graph LR
    A[圖片檔案] --> B[GeminiImageRecognizer]
    B --> C[Gemini API]
    C --> D[JSON 回應]
    D --> E[ResponseParser]
    E --> F[PassportRecognitionResult]
    F --> G[使用者]
```

## 依賴關係

```mermaid
graph TD
    A[PassportRecognitionController] -->|依賴| B[GeminiImageRecognizer]
    A -->|依賴| C[ResponseParser]
    C -->|建立| D[PassportRecognitionResult]
    B -->|使用| E[google-generativeai SDK]
    C -->|使用| F[Python json 模組]
```
