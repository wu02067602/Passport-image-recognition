# 護照辨識系統 - 類別圖

本文件展示護照辨識系統的類別結構與關係。

## 系統類別圖

```mermaid
classDiagram
    class GeminiImageRecognizer {
        -MAX_IMAGE_SIZE_MB: int
        -MAX_IMAGE_SIZE_BYTES: int
        -model: GenerativeModel
        +__init__(api_key: str, model_name: str)
        +recognize(image_path: str, prompt: str) str
        -_validate_image_size(image_path: str) None
    }

    class PassportRecognitionController {
        -recognizer: GeminiImageRecognizer
        -parser: ResponseParser
        -DEFAULT_PROMPT_TEMPLATE: str
        +__init__(recognizer: GeminiImageRecognizer, parser: ResponseParser)
        +recognize_passport(image_path: str, custom_prompt: str) PassportRecognitionResult
        +batch_recognize_passports(image_paths: list, custom_prompt: str) Dict
    }

    class ResponseParser {
        +parse(response_text: str) PassportRecognitionResult
        +parse_batch(responses: Dict) Dict
        -_extract_json_from_text(text: str) str
    }

    class PassportRecognitionResult {
        +passport_number: str
        +confidence: str
        +raw_response: str
        +__init__(passport_number: str, confidence: str, raw_response: str)
        +is_found() bool
        +to_dict() Dict
    }

    PassportRecognitionController --> GeminiImageRecognizer : 使用
    PassportRecognitionController --> ResponseParser : 使用
    ResponseParser --> PassportRecognitionResult : 建立
    PassportRecognitionController --> PassportRecognitionResult : 回傳
```

## 類別說明

### GeminiImageRecognizer
負責與 Google Gemini API 互動，處理圖片讀取、驗證和辨識功能。

### PassportRecognitionController
控制器類別，協調整個辨識流程，管理辨識器和解析器的互動。

### ResponseParser
解析 LLM 回傳的文字內容，將 JSON 格式字串轉換為結構化的結果物件。

### PassportRecognitionResult
封裝護照辨識結果的資料類別，提供結果查詢和轉換功能。
