# 護照辨識系統 - 類別圖

本文件展示護照辨識系統的類別結構與關係。

## 類別圖

```mermaid
classDiagram
    class PassportController {
        -ImageEncoder image_encoder
        -VisionAnalyzer vision_analyzer
        -ResultParser result_parser
        +__init__(model_name: str)
        +recognize_passport(image_path: Path) dict
        +recognize_single_field(image_path: Path, field: PassportField, custom_prompt: str) Any
        +get_supported_formats() set
    }
    
    class PassportService {
        -VisionAnalyzer vision_analyzer
        -ResultParser result_parser
        +__init__(model_name: str)
        +recognize_from_base64(base64_image: str) dict
    }
    
    class PassportBatchProcessor {
        -PassportController controller
        -set supported_formats
        +__init__(model_name: str)
        +scan_directory(directory_path: Path) list
        +process_single_image(image_path: Path) dict
        +process_directory(directory_path: Path, output_csv: Path) None
    }
    
    class ImageEncoder {
        +SUPPORTED_FORMATS: set
        +encode_image(image_path: Path) str
        +get_mime_type(image_path: Path) str
    }
    
    class VisionAnalyzer {
        -Client client
        -str model_name
        +MIME_TYPE_MAP: dict
        +__init__(model_name: str)
        +analyze_field(image_path: Path, field: PassportField, custom_prompt: str) str
        +analyze_all_fields(image_path: Path) dict
        +analyze_field_from_bytes(img_bytes: bytes, field: PassportField, custom_prompt: str) str
        +analyze_all_fields_from_bytes(img_bytes: bytes) dict
        -_get_image_mime_type(img_bytes: bytes) str
    }
    
    class ResultParser {
        +parse_llm_response(response: str) dict
        +parse_all_fields(raw_results: dict) dict
        +extract_value(parsed_response: dict) Any
        +get_confidence(parsed_response: dict) float
        -_clean_response(response: str) str
    }
    
    class PromptTemplates {
        +COMMON_PREFIX: str
        +TEMPLATES: dict
        +get_prompt(field: PassportField) str
        +get_all_fields() list
    }
    
    class PassportField {
        <<enumeration>>
        CHINESE_NAME
        ENGLISH_NAME
        NATIONALITY
        PASSPORT_NUMBER
        SEX
        DATE_OF_BIRTH
        DATE_OF_EXPIRY
    }
    
    class ParseError {
        <<exception>>
    }
    
    PassportController --> ImageEncoder : 使用
    PassportController --> VisionAnalyzer : 使用
    PassportController --> ResultParser : 使用
    PassportService --> VisionAnalyzer : 使用
    PassportService --> ResultParser : 使用
    PassportBatchProcessor --> PassportController : 使用
    VisionAnalyzer --> PromptTemplates : 使用
    VisionAnalyzer --> PassportField : 使用
    ResultParser --> PassportField : 使用
    ResultParser --> ParseError : 拋出
    PromptTemplates --> PassportField : 管理
```

## 類別說明

### PassportController
主控制器類別，負責協調所有模組的交互，提供完整的護照辨識流程。支援從本地圖片檔案進行護照辨識。

### PassportService
護照辨識服務類別，專門處理從 BASE64 編碼圖片進行辨識的場景，適用於 API 服務。與 PassportController 不同的是，它不依賴 ImageEncoder，直接處理位元組資料。

### PassportBatchProcessor
護照批次處理器類別，負責掃描目錄下的所有圖片檔案，批次進行護照辨識，並將結果輸出為 CSV 檔案。使用 PassportController 進行實際的辨識工作。

### ImageEncoder
圖片編碼器，負責將圖片檔案轉換為 base64 編碼字串和取得 MIME 類型。

### VisionAnalyzer
圖像理解分析器，使用 Google Gemini API 對護照圖片進行文字辨識與理解。支援從檔案路徑或位元組資料進行分析。

### ResultParser
結果解析器，負責將 LLM 返回的 JSON 字串解析成結構化的護照資料。

### PromptTemplates
提示詞模板管理類別，儲存和管理護照辨識各欄位的提示詞模板。

### PassportField
護照欄位枚舉，定義護照中需要辨識的各個欄位類型。

### ParseError
自訂異常類別，用於處理解析過程中發生的錯誤。
